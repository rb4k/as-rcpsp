<% provide(:title, 'Übersicht der Vorgänge') %>
<h1>Übersicht der Vorgänge</h1>

<% if @project.zwt!=nil %>
    Die minimale Projektdauer beträgt <strong><%=  @project.zwt %></strong> Zeiteinheiten. <br> <br>
<% end %>

<table id="procedures" class="table table-hover">
  <thead>
  <tr>
    <th>Name</th>
    <th>Kapazitätsbedarf</th>
    <th>Vorgangsdauer</th>
    <th>FA*</th>
    <th>SA*</th>
    <th>FE*</th>
    <th>SE*</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>
  </thead>
  <tbody>

<% @procedures.each do |procedure| %>
  <tr>
    <td><%= procedure.name %></td>
    <td><%= procedure.kapabe %></td>
    <td><%= procedure.prot %></td>
    <td><%= procedure.fa %></td>
    <td><%= procedure.sa %></td>
    <td><%= procedure.fe %></td>
    <td><%= procedure.se %></td>
    <td><%= link_to 'Anzeigen', procedure %></td>
    <td><%= link_to 'Ändern', edit_procedure_path(procedure) %></td>
    <td><%= link_to 'Löschen', procedure, method: :delete, data: { confirm: 'Sind sie sicher?' } %></td>
  </tr>
<% end %>
</tbody>
</table>

<br />



<%= button_to 'Neuer Vorgang anlegen', new_procedure_path, :class => "button", :method => :get %>
<%= button_to 'Zurück zur Projektplanung', rcpsp_path, :class => "button", :method => :get %>

<br />

<thead>
*FA=frühester Anfangszeitpunkt, SA=spätester Anfangszeitpunkt, FE=frühester Endzeitpunkt, SE=spätester Endzeitpunkt
</thead>

<br/>

